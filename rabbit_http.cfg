% This is the environment configuration the RMQ HTTP adpater

% TODO: At some stage this kind of file should go into
% a directory like /etc/rabbitmq/plugins rather than into the
% root of this plugins directory

% {listeners, [{"0.0.0.0", 55672}]}.

{rabbit_http_conf, "plugins/rabbit_http/ebin/httpd.conf.tmp"}.

{default_vhost, <<"/">>}.
